RewriteEngine On


# If the start of the URL doesn't match one of these...

RewriteCond $1 !^(main\.css|index\.php|img|uploads|css|js)


# Route through index.php

RewriteRule ^(.*)$ index.php/$1 [L]
 
